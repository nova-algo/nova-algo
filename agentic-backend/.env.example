# Privy Authentication
PRIVY_APP_ID=your_privy_app_id
PRIVY_APP_SECRET=your_privy_app_secret
PRIVY_WALLET_ID=your_wallet_id  # Optional, will create new if not provided
PRIVY_VERIFICATION_KEY=your_verification_key  # Public key for verification

# AI Model Configuration
OPENAI_MODEL=o3-mini  # or your preferred model
OPENAI_API_KEY=your_openai_api_key
# GEMINI_API_KEY=your_gemini_api_key
# GEMINI_MODEL=gemini-2.0-flash
# DEEPSEEK_API_KEY=your_deepseek_api_key
# DEEPSEEK_MODEL=deepseek-chat

# Application Security
AUTH_SECRET=your_auth_secret  # Generate a strong random string
SECRET_KEY=your_secret_key    # Generate a strong random string

# Allora Integration
ALLORA_API_KEY=your_allora_api_key
AGENT_ID=your_agent_id
NETWORK_ID=monad-testnet  # or your preferred network

# CDP Configuration
CDP_API_KEY_NAME=your_cdp_api_key_name
CDP_API_KEY_PRIVATE_KEY=your_cdp_private_key  # Keep this secure

# To generate secure random strings for AUTH_SECRET and SECRET_KEY, use:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
